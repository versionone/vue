import * as Spectrum from './spectrum';

export const watermelon = Spectrum.watermelon;
export const strawberry = Spectrum.strawberry;

// ...export all other colors in color picker (may be pulled from other files or hard-coded hex values)
